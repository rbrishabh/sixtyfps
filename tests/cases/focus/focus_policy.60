/* LICENSE BEGIN
    This file is part of the SixtyFPS Project -- https://sixtyfps.io
    Copyright (c) 2020 Olivier Goffart <olivier.goffart@sixtyfps.io>
    Copyright (c) 2020 Simon Hausmann <simon.hausmann@sixtyfps.io>

    SPDX-License-Identifier: GPL-3.0-only
    This file is also available under commercial licensing terms.
    Please contact info@sixtyfps.io for more information.
LICENSE END */


TestCase := Rectangle {
    width: 400phx;
    height: 400phx;

    callback enable_click_to_focus();
    enable_click_to_focus => {
       scope.focus_policy = FocusPolicy.click_to_focus;
    }
    scope := FocusScope {
    }

    property <bool> scope-has-focus: scope.has-focus;
}

/*
```rust
let instance = TestCase::new();
assert!(!instance.get_scope_has_focus());

sixtyfps::testing::send_mouse_click(&instance, 150., 100.);
assert!(!instance.get_scope_has_focus());

instance.emit_enable_click_to_focus();

sixtyfps::testing::send_mouse_click(&instance, 150., 100.);
assert!(instance.get_scope_has_focus());
```

```cpp
auto handle = TestCase::create();
const TestCase &instance = *handle;
assert(!instance.get_scope_has_focus());

sixtyfps::testing::send_mouse_click(&handle, 150., 100.);
assert(!instance.get_scope_has_focus());

instance.emit_enable_click_to_focus();

sixtyfps::testing::send_mouse_click(&handle, 150., 100.);
assert(instance.get_scope_has_focus());
```

```js
var instance = new sixtyfps.TestCase();
assert(!instance.scope_has_focus);

instance.send_mouse_click(150., 100.);
assert(!instance.scope_has_focus);

instance.enable_click_to_focus();

instance.send_mouse_click(150., 100.);
assert(instance.scope_has_focus);
```
*/
